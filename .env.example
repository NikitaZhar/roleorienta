# Локальные значения для профиля local-pilot (docker compose).
# Только для локальной разработки — не использовать в реальных окружениях.
# Скопируйте в .env и при необходимости измените.
DB_NAME=roleorienta
DB_USERNAME=roleorienta
DB_PASSWORD=roleorienta
RABBITMQ_USERNAME=roleorienta
RABBITMQ_PASSWORD=roleorienta
S3_ACCESS_KEY=roleorienta
S3_SECRET_KEY=roleorienta-secret
