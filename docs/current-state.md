# Текущее состояние roleorienta

Короткий документ «как устроено сейчас» (регламент сессии §7). Читается в начале каждой
сессии. История решений — в [project-notes](project-notes.md), правила — в
[рабочем контракте](working-contract.md) и [регламенте сессии](session-protocol.md).

**Обновлено:** 2026-09-25, после §98.
**Срезов с последнего аудита: 11** (последний аудит — §87; аудит кода — следующий шаг, после исправлений документации §98).

## Назначение и границы

Цель (ADR-19, ADR-20; [план развития](roleorienta-plan-korrektirovka.md)): приложение само, без
людей, находит компании заданных регионов, их карьерные страницы и вакансии на них; сначала ядро,
потом всё остальное. Источники компаний по порядку: открытые реестры юрлиц и индексы систем найма;
площадки — только как источник названий компаний и с разрешением; ручной ввод компании — третьим, по желанию клиента. Признака
«скрытая» нет. Сейчас работают вход «от вендора» (Workday, Personio по индексу Common Crawl) и
замер пути «от региона» для Словакии (§95–§96); сбор, нормализация, лента REST и ежедневный
дайджест по email (§88) — построены.

## Стек и запуск

Java 21, Spring Boot 4.1.1, PostgreSQL + Flyway (миграции V1–V32, ведёт `job-api`), RabbitMQ
(transactional outbox, publisher confirms, DLQ), Maven multi-module, Testcontainers в тестах,
CI — GitHub Actions `mvn -B -ntp verify`. Почта — Spring Mail (SMTP; локально Mailpit, письма на
http://localhost:8025). Локально: `./scripts/dev-up.sh` (Postgres, RabbitMQ, Mailpit
в Docker), `./scripts/run-api.sh` (:8080), `./scripts/run-worker.sh` (:8081).

## Модули

- **core** — доменные сущности и перечисления (`JobPosting`, `Source`, `Provider`, `Company`,
  `EmployerCandidate` + `EmployerCandidateState`, `CrawlRun/CrawlTask`, `PostingRevision`,
  `SalaryPeriod/Basis`, `SeniorityLevel`, `WorkModality` …).
- **job-api** — REST: `posting` (лента, карточка), `auth`, `saved`, `subscription`,
  `notification`, `application` (отклики/собеседования), `report`, `discovery` (админ-очередь
  кандидатов — к замене автоматическим подключением, ADR-20). Владеет схемой БД.
- **job-worker** — фоновая работа: `scheduling` (планировщик источников под leader-lock),
  `outbox`/`messaging`/`idempotency` (доставка заданий), `discovery` (обнаружение),
  `collect` (сбор и обогащение), `normalize`, `extract`, `adapters` (Greenhouse, Workday, Personio),
  `region` (путь «от региона»: реестр RPO, сайт по IČO, карьерная страница, §95), `http` (единственный HTTP-клиент), `digest` (сопоставление подписок; ежедневный дайджест по email — `DigestRun`, §88), `lock`, `xml`.

## Путь данных

1. **Обнаружение.** `CcHarvestScheduler` (включён, §72) читает индекс Common Crawl по входам
   `CcInput` (`app.discovery.cc.inputs`, §91): Workday `*.myworkdayjobs.com`, Personio
   `*.jobs.personio.de/.com`; у входа свой курсор → доски в `harvested_board`; новая страница — только когда очередь
   `NEW` меньше `max-fan-out` (§73). `BoardFanOut` ставит `DISCOVER_EMPLOYER` через outbox — до `max-fan-out` досок за
   проход, из входов по очереди (§92), чтобы очередь одного входа не задерживала другие.
2. **Гейт.** `DiscoverEmployerJobHandler`: читает первую страницу ленты; рынок — по фасету
   стран, иначе фасету локаций, иначе локациям вакансий первой страницы (§56–§59, §73).
   Перед авто-подключением — принадлежность доски (A2, §79): `SourceAdapter.boardProfile`
   (Workday: тенант + `og:description` страницы доски) сверяет `BoardOwnershipProperties`;
   нет описания, признак агентства, тенант не назван — `PENDING`.
   Итог кандидата: `CONFIRMED` (HIGH → `EmployerSourceRegistrar` заводит `Source` ACTIVE;
   компания — по ключу `Company.identityKey` «провайдер:тенант», имя — из описания доски, §80),
   `OUT_OF_MARKET`, `UNREACHABLE` (403/404/410/422, §74), `PENDING` (ручная проверка — к замене
   автоматической перепроверкой, ADR-20).
3. **Сбор.** Планировщик (`DiscoverPageEnqueuer`) → `DISCOVER_PAGE` (`DiscoverPageJobHandler`: лента только рынка —
   фильтр Workday `appliedFacets`, все публикации сохраняются) → `FETCH_POSTING` только для
   ниши (`NicheFilterProperties`) и в пределах дневного бюджета на источник (`PostingIntake`, §63).
4. **Обогащение.** `PostingEnricher`: `LocationNormalizer` (город/страна/формат, доп. локации),
   `SalaryNormalizer.resolve` (структурная зарплата, иначе `SalaryTextExtractor` по тексту, §66),
   `ExperienceExtractor` (уровень — только из заголовка, годы — из описания, §70), языки и
   навыки (`PostingRequirementWriter`), история изменений (`PostingRevisionRecorder`).
5. **Лента.** `GET /api/v1/postings`: фильтры `PostingFilter` (формат, уровень, страна с доп.
   локациями, `minSalary` с «от X», `postedFrom`), порядок `FeedPaging.sort` = `ID` | `POSTED`
   (keyset «дата + id» — `PostedKeyset`, §70); карточка `GET /api/v1/postings/{id}`. JSON сгруппирован
   (§77): строка — `{head, facts, viewer, coverage}`, карточка — `{head, facts, description,
   requirements, coverage}`; `facts` = `{location, salary, experience, timeline}`. Покрытие (A5,
   §81): `CoverageAssessment` на публикацию, нет записи — `UNKNOWN`; фильтр `coverage=SITE_ONLY`
   («только скрытые») / `UNKNOWN`. **Покрытие исключено из продукта (ADR-20)** — поле, фильтр и
   V29 удаляются отдельным срезом (меняет API).
6. **Покрытие (A5, §82; выключено §91, к удалению — ADR-20).** `CoverageCheck` (worker, leader-lock 1005, тик `CoverageTrigger`,
   `app.coverage`): работодатели с австрийскими публикациями без свежей оценки → один запрос к
   karriere.at (`KarriereClient`, HTML-выдача, только активные) → `CoverageMatcher`: тот же
   заголовок у того же работодателя — `BOTH`; у другого юрлица, «почти такой же» заголовок,
   неполная выдача — `UNKNOWN` с причиной; иначе `SITE_ONLY` «не найдена на karriere.at при
   проверке [дата]»; нет выдачи (404) — `UNKNOWN` с датой (§83). Словакия — «не проверено».
   Доска другого бренда (`accenture/AvanadeCareers` → «Avanade», `BoardBrand`, §86) ищется по
   бренду — свой запрос. Компании со старым именем-slug переименовывает `CompanyNameBackfill` (lock 1006, §83).
7. **Дайджест (A7, §88).** `DigestTrigger` (cron `app.digest.mail.cron`, 06:50 Europe/Bratislava) →
   `DigestRun` (lock 1007): подписчику с новыми вакансиями его компаний (сначала «только с сайта» — порядок по покрытию, к удалению, ADR-20)
   или изменениями (уведомления) в окне «с прошлого письма (иначе — с подписки) до сейчас» —
   письмо `digest_delivery` (V31) `PENDING` → SMTP → `SENT`; сбой — повтор, после 3 попыток `FAILED`.

## Соглашения кода (кратко; полностью — контракт §3)

- ≤5 параметров у методов/конструкторов и ≤5 полей у записей, **включая DTO** — группировать во
  вложенные записи (`PostingFilter`, `FeedPaging`, `PostingDtos.Facts`, `*Properties`) или выносить
  компонент. Проверяет Checkstyle (`config/checkstyle/checkstyle.xml`, фаза validate, §78); он же —
  имена переменных, параметров, полей не короче двух символов, включая индексы циклов (§88).
- Архитектура проверяется тестами ArchUnit (`ArchitectureTest` в job-api и job-worker, §86): нет
  циклов пакетов, HTTP воркера — только `SourceHttpClient`, XML — только `SafeXml`, контроллеры
  без репозиториев.
- Ключи leader-lock — реестр в JavaDoc `PostgresLeaderLock` (1001–1007); новый ключ — туда же.
- Настройки — `@ConfigurationProperties`-записи с `@DefaultValue`, регистрируются в
  `JobWorkerApplication`; значения и комментарии — в `application.yml`.
- Репозитории — только запросы; логика (курсоры, преобразования) — в сервисах и записях-значениях.
- Исходящие запросы воркера — только `SourceHttpClient` (SSRF-защита, темп на домен,
  `Retry-After`, потолок тела 5 МБ); XML — только `SafeXml`.
- Неизвестное не угадывается: `UNKNOWN`/`null` вместо догадки (зарплата, уровень, формат).
- JPQL: nullable-параметр даты — через вычисленный в Java признак (§68).
- Тесты: модульные на заглушках + интеграционные на Testcontainers; `JobWorkerApplicationTests`
  выключает обход Common Crawl.

## Стенды (`target/*.sh`, вне git)

`stand-cc.sh` — обнаружение Common Crawl (15 мин, отчёт `stand-cc.txt`); `stand-salary.sh` —
перезапрос деталей 5 досок SK/AT без ограничения ниши (`stand-salary.txt`); `stand-feed.sh` —
лента через REST (`stand-feed.txt`). Скрипты сами запускают и останавливают воркер; нужны
`dev-up.sh`, для REST — `run-api.sh`. `stand-coverage.sh` — один проход проверки покрытия и
переименования компаний (~2 мин, `coverage.txt`; покрытие — к удалению, ADR-20); `stand-brand.sh` — перепроверка досок другого
бренда (~3 мин, `stand-brand.txt`); `stand-region.sh` — замер пути «от региона» (RPO → сайт → карьерная страница; MIN=20 мин,
`stand-region.txt`, нужна V32); `stand-personio.sh` — вход Personio: доски из индекса, гейт, сбор
(MIN=15 мин, `stand-personio.txt`); `stand-digest.sh` — дайджест пользователю-стенду, письма из
Mailpit (~2 мин, `stand-digest.txt`, нужна V31); требует применённой V29 (дайджест читает покрытие — к удалению, ADR-20) (её применяет
`run-api.sh`).

## Правовые основания источников (A24)

Что читаем, на каком основании и что храним. Новый источник — строка сюда до включения в сбор.
Проверка — robots.txt и условия использования на дату проверки (не юридическое заключение);
подробности и цитаты — project-notes §90.

| Источник | Как читаем | Основание (дата проверки) | Что храним | Итог |
|---|---|---|---|---|
| Индекс Common Crawl (`index.commoncrawl.org`) | CDX API, адреса досок `*.myworkdayjobs.com` | Terms of Use Common Crawl, открытый доступ (2026-09-23, протокол Этапа 0 §3) | адрес доски | разрешено |
| Ленты Workday тенантов (`*.myworkdayjobs.com/wday/cxs`) | публичный JSON сайта вакансий работодателя, без ключа, темп на домен | robots.txt тенантов разрешает сайты вакансий, закрыт только `/refreshFacet/`, `/wday/cxs/` не закрыт (lilly, accenture; 2026-09-25). Условия Workday (workday.com/legal/site-terms) касаются workday.com и «Workday APIs, доступных через их сайты» — сайты вакансий на myworkdayjobs.com в них не названы; своих условий на страницах вакансий нет. Тексты вакансий принадлежат работодателю | вакансия целиком, включая текст описания; текст отдаётся в карточке API | серая зона: запрета не найдено; риск — отнесение `cxs` к «Workday APIs» и перепубликация текста |
| karriere.at (Австрия) | не читаем (проверка покрытия выключена §91, код к удалению, ADR-20) | **Nutzungsbedingungen п. 2.8.3: «Sämtliche Formen der automatisierten Auswertung unserer Plattform sind verboten»**, действуют для всех посетителей (п. 1.3) (2026-09-25). Прежняя проверка (§82) смотрела только AGB | факт наличия: id, состояние, дата | **запрещено** без письменного разрешения |
| profesia.sk (Словакия) | не читаем | Условия Alma Career для пользователей (действуют с 01.01.2025; «пользователь» — и незарегистрированный посетитель): запрещены автоматическое или массовое чтение и копирование (п. 4.7 e), автоматическая обработка и data mining (п. 4.11 a, d), копирование без согласия (п. 5.6). Выгрузка XML — только по письменному договору партнёрства (2026-09-25) | — | **запрещено** без договора |
| Personio (`<аккаунт>.jobs.personio.de/.com/xml`) | XML-лента вакансий работодателя, без ключа, темп на домен | Лента — официальная функция Personio для размещения вакансий на других сайтах; работодатель включает её сам в настройках карьерной страницы (developer.personio.de; support.personio.de). robots.txt доски — 404 (ограничений нет). Условий, запрещающих чтение ленты, не найдено (2026-09-25) | вакансия целиком, включая текст описания | разрешено (публичная лента по выбору работодателя) |
| Реестр юрлиц Словакии RPO (`api.statistics.sk/rpo`, rpo.minv.sk) | открытый REST API без ключа; название, адрес, основной вид деятельности (SK NACE) — сайта нет | CC BY 4.0, нужна атрибуция (rpo.minv.sk/rpo-api-doc.html, 2026-09-25) | IČO, название, город, вид деятельности (`registry_company`, §95) | разрешено; атрибуция «Zdroj: RPO, CC BY 4.0» — при показе данных |
| Firmenbuch Австрии, HVD (justizonline.gv.at) — не читаем | API по бесплатному ключу (регистрация) | CC BY 4.0, набор высокой ценности по регламенту ЕС 2023/138 (2026-09-25) | — | разрешено |
| Wikidata — не читаем | SPARQL; компании с сайтом (P856) и отраслью | CC0 | — | разрешено |
| Сайты компаний Словакии (главная, контакты, раздел вакансий; §95) — замер | разметка, которую сайт публикует для поисковиков | публичные страницы; robots.txt пока не проверяется (замер на десятках сайтов) — до промышленного сбора обязателен | адрес сайта и карьерной страницы, число вакансий | замер; сбор — после проверки robots.txt |
| Lever (`api.lever.co/v0/postings/<аккаунт>`) — не читаем | публичный Postings API | в документации Lever: опубликованные вакансии «may be scraped by third parties» (github.com/lever/postings-api, 2026-09-25) | — | разрешено; адаптера нет |
| Greenhouse (`boards-api.greenhouse.io/v1/boards/<доска>`) | публичный Job Board API (адаптер есть, во вход обнаружения не включён) | «Job Board data is publicly available», GET без ключа (developers.greenhouse.io, 2026-09-25); ограничений не указано | вакансия с описанием | разрешено |

## Где мы сейчас (точка остановки 2026-09-25, после §98)

**План переработан целиком (§97)** — [план развития](roleorienta-plan-korrektirovka.md): ядро
K1–K8 «компании региона → сайт → карьерная страница → вакансии», всё автоматически; вакансия есть
только если она на карьерной странице; нет страницы — компания пропускается; страницы без
структурной разметки не читаются; размер и профиль компании не критерии; глянец — только после
того, как ядро работает (критерий — план §3). Проверка реализованного — план §5.
**§98:** ручной ввод компании — третий источник после реестров и площадок, опция по желанию клиента
(план E4); признак «скрытая» и покрытие исключены; бизнес-ТЗ и технический документ приведены к
плану (ADR-20).

**Ближайшее:** аудит кода по регламенту (счётчик 11). Затем ядро: K1 — очередь всех юрлиц региона Словакии порциями (без фильтра по виду
деятельности); K2 — исследование метода поиска сайта; K7 — отчёт измерителей. Стенд
`stand-region.sh` (§95–§96) — замер на выборке, пока с фильтром по виду деятельности.

**Открытые решения владельца:** разрешение на изменение API при удалении кода покрытия (поле и
фильтр `coverage` в ленте, V29, пакет `coverage`, строка в дайджесте) и очереди подтверждения
кандидатов (`/api/v1/admin/employer-candidates`) — план §5, строки «Вне продукта» и «Вне ядра».

## Статус плана

См. план §5 — таблица «что реализовано» по пунктам ядра K1–K8.
